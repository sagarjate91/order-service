spring:
      application:
                 name: order-service
      datasource:
             url: jdbc:mysql://localhost:3306/flipkart
             username: root
             password: admin
             driver-class-name: com.mysql.cj.jdbc.Driver
      jpa:
        hibernate:
             ddl-auto: update

server:
     port: 8081